import { InputTextTrimDirective } from './input-text-trim.directive';

describe('InputTextTrimDirective', () => {
  it('should create an instance', () => {
    const directive = new InputTextTrimDirective();
    expect(directive).toBeTruthy();
  });
});
